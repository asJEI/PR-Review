{
  "name": "pr-review",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:shared": "pnpm --filter @pr-review/shared build",
    "build:github": "pnpm --filter @pr-review/github build",
    "build:diff-parser": "pnpm --filter @pr-review/diff-parser build",
    "build:context-builder": "pnpm --filter @pr-review/context-builder build",
    "build:context-compressor": "pnpm --filter @pr-review/context-compressor build",
    "build:context-relevance": "pnpm --filter @pr-review/context-relevance build",
    "build:prompt-builder": "pnpm --filter @pr-review/prompt-builder build",
    "build:ai": "pnpm --filter @pr-review/ai build",
    "test": "node scripts/build.mjs && cd packages/diff-parser && npx vitest run && cd ../context-builder && npx vitest run && cd ../context-compressor && npx vitest run && cd ../context-relevance && npx vitest run && cd ../focused-diff && npx vitest run && cd ../prompt-builder && npx vitest run && cd ../ai && npx vitest run",
    "smoke:context": "pnpm run build && node packages/context-builder/scripts/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "ignoredBuiltDependencies": ["esbuild"]
  }
}
