# 工程规则
你正在构建一个基于 AI 的 Pull Request 审查助手。
项目目标：
构建一个具备上下文感知能力的 AI 代码审查系统，帮助开发者理解 PR 中的变更、识别工程风险，并生成高质量的审查建议。

核心理念：
本项目并非简单的对 LLM 封装。
系统应侧重于：
* 工程语义
* 上下文感知分析
* 减少误报
* 理解代码关系
* 类评审者的推理能力

架构原则：
* 强模块化设计
* 单仓库结构
* 职责清晰分离
* 每个包应只负责单一职责
* 共享类型必须集中管理

技术栈：
* TypeScript
* Node.js
* pnpm workspace
* Next.js 前端
* Express/Fastify 后端
* Octokit 用于 GitHub API
* AI 提供商抽象层

代码风格：
* 优先采用函数式编程
* 避免深度嵌套的逻辑
* 强类型
* 除非必要，否则避免使用 `any`
* 保持文件小巧且易于组合
* 优先采用纯工具函数

重要提示：
切勿过度设计。
首先专注于构建一个稳定的最小可行产品（MVP）。

当前 MVP 范围：
1. 解析 GitHub PR 链接
2. 获取 PR 数据
3. 解析代码差异
4. 构建代码上下文
5. 生成 AI 摘要
6. 生成代码审查建议

非MVP功能（目前避免实现）：
* 身份验证
* 多用户系统
* 数据库持久化
* Webhooks
* GitHub应用集成
* 实时协作
* 复杂的缓存机制

生成代码时：
* 简要说明架构决策
* 优先考虑可维护性而非花哨的技巧
* 保持生产就绪的实现风格
* 避免使用虚假的占位符实现
* 避免不必要的抽象
