/** Shared language preference for all review agents. */
export const LANGUAGE_PREFERENCE = `- Prefer Simplified Chinese (简体中文) for all descriptive text in your response (summaries, rationales, recommendations, comment bodies, etc.)
- JSON field names and enum values (e.g. severity, confidence, critical|high|medium|low) must stay in English as defined in the output schema
- Keep file paths, symbol names, and code identifiers unchanged`;
